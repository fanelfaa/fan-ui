language: typescript
layer: library
stack: frontend

tasks:
  build:
    command: 'pnpm run build'
    inputs:
      - 'src/**'
      - 'tsup.config.ts'
    outputs:
      - 'dist/**'
  dev:
    command: 'pnpm run dev'
  typecheck:
    command: 'tsc --noEmit'