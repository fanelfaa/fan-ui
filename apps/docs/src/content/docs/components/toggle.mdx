---
title: Toggle
description: A button-like component for toggling between two states, commonly used for formatting options.
category: Form & Input
updatedDate: 2026-05-23
---

import { Toggle } from "@ui/solid";
import ToggleBasicDemo from "@components/toggle-demo/ToggleBasicDemo.tsx";
import ToggleIndicatorDemo from "@components/toggle-demo/ToggleIndicatorDemo.tsx";

# Toggle

A button-like component for toggling between on and off states, commonly used for formatting options like bold, italic, or other binary settings.

<a
  href="https://ark-ui.com/docs/components/toggle"
  target="_blank"
  rel="noopener noreferrer"
  class="inline-flex items-center gap-1.5 mt-4 text-sm text-muted-foreground hover:text-foreground transition-colors"
>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    width="14"
    height="14"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
  >
    <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
    <polyline points="15 3 21 3 21 9" />
    <line x1="10" x2="21" y1="14" y2="3" />
  </svg>
  Docs
</a>

<ToggleBasicDemo client:load />

```tsx
import { Toggle } from "~/components/toggle";

export function ToggleDemo() {
  return (
    <div class="flex items-center gap-4">
      <Toggle>
        <BoldIcon />
      </Toggle>
      <Toggle defaultPressed>
        <BoldIcon />
      </Toggle>
      <Toggle disabled>
        <BoldIcon />
      </Toggle>
    </div>
  );
}
```

## With Indicator

`ToggleIndicator` renders different content based on the toggle state — the `fallback` is shown when off, and the children are shown when on.

<ToggleIndicatorDemo client:load />

```tsx
import { Toggle, ToggleIndicator } from "~/components/toggle";

export function ToggleIndicatorDemo() {
  return (
    <div class="flex items-center gap-4">
      <Toggle>
        <ToggleIndicator fallback={<SunIcon />}>
          <MoonIcon />
        </ToggleIndicator>
      </Toggle>
      <Toggle defaultPressed>
        <ToggleIndicator fallback={<SunIcon />}>
          <MoonIcon />
        </ToggleIndicator>
      </Toggle>
    </div>
  );
}
```

## Sizes

Use the `size` prop to change the toggle size.

<div class="rounded-lg border border-border p-6">
  <div class="flex flex-wrap items-center gap-4">
    <Toggle size="sm" client:load>Sm</Toggle>
    <Toggle size="md" client:load>Md</Toggle>
    <Toggle size="lg" client:load>Lg</Toggle>
  </div>
</div>

```tsx
import { Toggle } from "~/components/toggle";

export function ToggleSizesDemo() {
  return (
    <div class="flex items-center gap-4">
      <Toggle size="sm">Sm</Toggle>
      <Toggle size="md">Md</Toggle>
      <Toggle size="lg">Lg</Toggle>
    </div>
  );
}
```

## Installation

### CLI

Run the following command to add the component to your project:

```bash
npx solidui-cli@latest add toggle
```

### Manual

<div class="space-y-3">
Install the dependency:
```bash
npm install tailwind-variants
```
</div>

<div class="space-y-3">
Create the recipe file at `src/components/recipes/toggle.ts`:
```tsx
import { tv, type VariantProps } from 'tailwind-variants'

export const toggleVariants = tv({
  slots: {
    root: "inline-flex items-center justify-center gap-2 rounded-md text-sm font-medium border border-input bg-transparent text-foreground hover:bg-accent hover:text-accent-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1 transition-colors data-[state=on]:bg-accent data-[state=on]:text-accent-foreground data-[state=on]:border-transparent data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
    indicator: "inline-flex items-center justify-center",
  },
  variants: {
    size: {
      sm: { root: "h-8 px-2.5" },
      md: { root: "h-9 px-3 py-1.5" },
      lg: { root: "h-10 px-6" },
    },
  },
  defaultVariants: {
    size: "sm",
  },
});

export type ToggleVariants = VariantProps<typeof toggleVariants>
```
</div>

<div class="space-y-3">
Create the component file at `src/components/toggle.tsx`:
```tsx
import { Toggle as ArkToggle } from '@ark-ui/solid/toggle'
import { createMemo, splitProps, type Component } from 'solid-js'
import { ToggleVariants, toggleVariants } from '../recipes/toggle'

const styles = toggleVariants()

const ToggleRoot: Component<ArkToggle.RootProps & ToggleVariants> = (props) => {
  const [local, others] = splitProps(props, ['class', 'size'])
  const rootClass = createMemo(() => styles.root({ class: local.class, size: local.size }))
  return <ArkToggle.Root class={rootClass()} {...others} />
}

const ToggleIndicator: Component<ArkToggle.IndicatorProps> = (props) => {
  const [local, others] = splitProps(props, ['class'])
  const indicatorClass = createMemo(() => styles.indicator({ class: local.class }))
  return <ArkToggle.Indicator class={indicatorClass()} {...others} />
}

export { ToggleRoot as Toggle, ToggleIndicator, toggleVariants }
```
</div>

## Usage

Import the components:

```tsx
import { Toggle, ToggleIndicator } from "~/components/toggle";
```

Basic:

```tsx
<Toggle>
  <BoldIcon />
</Toggle>
```

Default pressed:

```tsx
<Toggle defaultPressed>
  <BoldIcon />
</Toggle>
```

Controlled:

```tsx
const [pressed, setPressed] = createSignal(false);

<Toggle pressed={pressed()} onPressedChange={setPressed}>
  <BoldIcon />
</Toggle>
```

Disabled:

```tsx
<Toggle disabled>
  <BoldIcon />
</Toggle>
```

With Indicator:

`ToggleIndicator` renders different content based on the toggle state — the `fallback` is shown when off, and the children are shown when on.

```tsx
<Toggle>
  <ToggleIndicator fallback={<SunIcon />}>
    <MoonIcon />
  </ToggleIndicator>
</Toggle>
```

## API Reference

### Toggle (Root)

| Prop             | Type                      | Default | Description                           |
| ---------------- | ------------------------- | ------- | ------------------------------------- |
| defaultPressed   | boolean                   | —       | Initial state (uncontrolled).         |
| pressed          | boolean                   | —       | Controlled pressed state.             |
| onPressedChange  | `(pressed: boolean) => void` | —    | Callback when pressed state changes.  |
| size             | "sm" \| "md" \| "lg"      | "sm"    | Size of the toggle (h-8 / h-9 / h-10).|
| disabled         | boolean                   | false   | Whether the toggle is disabled.       |
| class            | string                    | —       | Custom CSS class.                     |

### ToggleIndicator

| Prop   | Type     | Default | Description                                 |
| ------ | -------- | ------- | ------------------------------------------- |
| fallback | `ReactNode` | —    | Content shown when toggle is not pressed.   |
| class  | string   | —       | Custom CSS class.                           |
