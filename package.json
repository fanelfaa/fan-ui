{
  "name": "@fan-ui/monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Solid.js component library built on Ark UI",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "typecheck": "pnpm -r run typecheck",
    "netlify:build": "pnpm --filter @fan-ui/core build && pnpm --filter @fan-ui/solid build && pnpm --filter docs-spa generate-routes && pnpm --filter docs-spa build",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "oxfmt": "^0.51.0",
    "oxlint": "^1.66.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "lightningcss"
    ]
  }
}
